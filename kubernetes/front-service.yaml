kind: Service
apiVersion: v1
metadata:
  name: front-svc
  namespace: ingress-nginx
spec:
  selector:
    app: front
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: https
      port: 443
      targetPort: https