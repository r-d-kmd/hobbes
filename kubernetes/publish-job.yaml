apiVersion: batch/v1
kind: Job
metadata:
  name: publish
spec:
  template:
    spec:
        containers:
        - name: publish
          imagePullPolicy: Always
          image: kmdrd/workbench
          env:
          - name: MASTER_KEY
            value: "Rno8hcqr9rXXs"
          - name: HOST
            value: "http://gateway-svc"
        restartPolicy: Never
  backoffLimit: 4